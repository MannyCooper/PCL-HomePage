<local:MyCard Title="test">
    <StackPanel Margin="25,40,23,15">
        <TextBlock Margin="0,0,0,4"
                   Text="咳咳，这里是 MannyCooper。这是一个示例卡片。之后会出现有趣的内容哦~" />
        <TextBlock Margin="0,0,0,4" Foreground="#FF0000"
                   Text="这个文本使用红色前景色,嘿嘿嘿。" />
        <Button Content="点击我（无事发生）" Width="100" Height="30" Margin="0,10,0,0" />
    </StackPanel>
</local:MyCard>
